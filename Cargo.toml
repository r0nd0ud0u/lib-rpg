[package]
name = "rust-rpg"
version = "0.1.0"
authors = [""]
edition = "2018"

[dependencies]
cxx = "1.0"
rand = "0.8.5"

[build-dependencies]
cxx-build = "1.0"

[lib]
crate-type = ["staticlib"]

[profile.release]
debug = true
panic = "abort"

[profile.dev]
panic = "abort"
